# projetos-industria
Projetos Ind�stria

## Spec-Driven Development (GitHub Spec Kit)

Este repositório inclui [GitHub Spec Kit](https://github.com/github/spec-kit) integrado ao Cursor (`--integration cursor-agent`).

Skills disponíveis no Agent (slash):
- `/speckit-constitution` · `/speckit-specify` · `/speckit-plan` · `/speckit-tasks` · `/speckit-implement`
- Opcionais: `/speckit-clarify` · `/speckit-analyze` · `/speckit-checklist` · `/speckit-converge`

Infraestrutura em `.specify/` e skills em `.cursor/skills/speckit-*`.
