# projetos-industria

Monorepo de projetos industriais.

## Gestão Oficina de Valor (VMO)

MVP high-code de value tracking e portfólio: ver [`oficina-valor/README.md`](oficina-valor/README.md).

```bash
cd oficina-valor
# com Docker: docker compose up -d --build
# sem Docker: subir Postgres, depois api + web (instruções no README)
```

## Checklist Plataforma IA Industrial

App em `web/` + agente Nao em `nao/` (raiz).
